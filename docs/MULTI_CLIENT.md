# Multi-Client Setup

This page has been superseded by two more detailed, verified documents:

- **[docs/configuration.md](configuration.md)** — real, copy-pasteable configuration for Claude Code, Claude Desktop, OpenAI Codex CLI, GitHub Copilot (VS Code), and Cursor, using the actual `android-corporate-mcp` command produced by this project's npm distribution.
- **[docs/compatibility.md](compatibility.md)** — which clients, platforms, and distribution channels are actually tested versus expected-compatible, and the real runtime requirements.

The configuration examples previously on this page used placeholder JAR paths (`/path/to/android-corporate-mcp-server.jar`) and a Docker setup that does not currently build (see [troubleshooting.md](troubleshooting.md#docker-build-fails-to-find-the-jar)); they've been replaced by the pages above so the project has one accurate source for client configuration instead of two.

For the full tool inventory previously listed here, see [docs/tools.md](tools.md).
